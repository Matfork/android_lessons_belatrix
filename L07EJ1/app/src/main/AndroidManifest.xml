<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matt.l07ej1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActividadC"
            android:label="@string/title_activity_actividad_c"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ActividadE" />
        <activity android:name=".ActividadD" />

        <service
            android:name=".ServiceIntentEjemplo"
            android:exported="false" />

        <activity android:name=".CallService"></activity>
    </application>

</manifest>